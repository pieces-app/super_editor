name: golden_runner
description: Commands to test and update goldens in a Docker container
version: 0.2.0
homepage:

executables:
  goldens:

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  args: ^2.3.1
  meta: ^1.8.0
  path: ^1.8.3

flutter:
